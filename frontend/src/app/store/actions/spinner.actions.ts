import { createAction } from '@ngrx/store';

export const START_ROTATE_SPINNER = createAction('[App Component] Start Rotate Spinner');
export const STOP_ROTATE_SPINNER = createAction('[App Component] Stop Rotate Spinner');
